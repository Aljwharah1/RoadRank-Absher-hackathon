# 🇸🇦 RoadRank - Safe Driving AI System
## دليل شامل للمشروع

---

## 📋 نظرة عامة

**RoadRank** هو نظام ذكاء اصطناعي متكامل لتقييم سلامة القيادة. المشروع يوضح دورة ML كاملة:

1. ✅ **توليد البيانات الاصطناعية** (Synthetic Data Generation)
2. ✅ **معالجة وهندسة الميزات** (Feature Engineering)
3. ✅ **تدريب النماذج** (Model Training)
4. ⏳ **بناء API** (FastAPI Backend)
5. ⏳ **لوحة المعلومات** (React Frontend)

---

## 🚀 البدء السريع (Quick Start)

### الخطوة 1: تثبيت المكتبات

```bash
# تفعيل البيئة
source new/bin/activate

# تثبيت المكتبات
pip install -r requirements.txt
```

### الخطوة 2: توليد البيانات

```bash
python data_generator_v2.py
```

المخرجات:
- `telemetry_data.csv` - بيانات ثانية/ثانية
- `trip_summary.csv` - ملخص كل رحلة

### الخطوة 3: معالجة البيانات

افتح الـ Notebook:
```bash
jupyter notebook 01_data_exploration_and_features.ipynb
```

### الخطوة 4: تدريب النموذج

```bash
python 02_model_training.py
```

---

## 📂 شرح الملفات الرئيسية

### 1. `data_generator_v2.py`
**الهدف:** توليد بيانات اصطناعية واقعية

**ما يفعل:**
- ينشئ 200 رحلة بأنواع سائقين مختلفة
- يولد بيانات ثانية/ثانية (300-6000 صف لكل رحلة)
- يحسب مؤشر السلامة لكل رحلة

**المخرجات:**
```
telemetry_data.csv
├─ trip_id: معرف الرحلة
├─ speed_kmh: السرعة
├─ acceleration_kmh2: التسارع
├─ harsh_brake: فرملة حادة (0/1)
├─ lane_change: تغيير مسار (0/1)
└─ ... و15 عمود آخر

trip_summary.csv
├─ trip_id: معرف الرحلة
├─ safe_driving_score: المؤشر (0-100)
├─ driver_category: التصنيف (آمن/معتدل/خطر)
├─ harsh_brakes_count: عدد الفرملات الحادة
└─ ... و20 عمود آخر
```

---

### 2. `01_data_exploration_and_features.ipynb`
**الهدف:** استكشاف البيانات و Feature Engineering

**الأقسام:**
1. **Import & Load** - تحميل البيانات
2. **Exploration** - فهم البيانات
3. **Feature Engineering** - إنشاء ميزات جديدة
4. **Scaling** - تطبيع البيانات
5. **Train-Test Split** - تقسيم البيانات
6. **Visualization** - رسوم بيانية
7. **Save** - حفظ البيانات المعالجة

**المخرجات:**
```
processed_data_with_features.csv    ← البيانات الكاملة مع الميزات الجديدة
X_train.csv, X_test.csv             ← الميزات
y_train.csv, y_test.csv             ← التصنيفات
*.png                               ← الرسوم البيانية
```

---

### 3. `02_model_training.py`
**الهدف:** تدريب وتقييم نماذج ML

**يفعل:**
- يدرب Random Forest Classifier
- يدرب Gradient Boosting Classifier
- يحسب: Accuracy, Precision, Recall, F1-Score
- ينشئ Confusion Matrix
- يحفظ النموذج الأفضل

**المخرجات:**
```
models/
├─ safe_driving_model_rf.pkl        ← نموذج Random Forest
├─ safe_driving_model_gb.pkl        ← نموذج Gradient Boosting
└─ model_metrics.json               ← مؤشرات الأداء
```

---

## 📊 شرح الخوارزميات والمفاهيم

### أنواع السائقين الـ 4

```python
DRIVER_PROFILES = {
    'safe': {
        'speed_variance': 2,              # تباين صغير = قيادة ثابتة
        'harsh_brake_probability': 0.005, # 0.5% فرملات حادة
        'lane_change_probability': 0.001  # 0.1% تغيير مسار
    },
    'moderate': {
        'speed_variance': 5,
        'harsh_brake_probability': 0.02,
        'lane_change_probability': 0.003
    },
    'aggressive': {
        'speed_variance': 8,
        'harsh_brake_probability': 0.08,
        'lane_change_probability': 0.008
    },
    'distracted': {
        'speed_variance': 10,
        'harsh_brake_probability': 0.06,
        'lane_change_probability': 0.012
    }
}
```

### حساب مؤشر السلامة

```python
score = 100
score -= harsh_brakes * 3         # كل فرملة حادة = -3 نقاط
score -= harsh_accels * 1.5       # كل تسارع حاد = -1.5 نقاط
score -= lane_changes * 0.5       # كل تغيير مسار = -0.5 نقاط
score -= speeding_pct * 0.8       # السرعة الزائدة = -0.8 نقاط
score += avg_congestion * 5       # الازدحام يعطي نقاط مكافأة

# النتيجة النهائية:
80-100 → "آمن"
50-79  → "معتدل"
<50    → "خطر"
```

### Confusion Matrix

```
                 الحقيقي
                 آمن  معتدل  خطر
التنب    آمن      45    5     0
ؤ        معتدل     2    38    3
ة        خطر      1    2     24

القطر الرئيسي = التنبؤات الصحيحة
بقية الخلايا = الأخطاء
```

---

## 🧠 الميزات المستخدمة في التدريب

### أنواع الميزات

```
1. ميزات السرعة
   ├─ avg_speed: متوسط السرعة
   ├─ max_speed: أقصى سرعة
   ├─ speed_excess: كم أكثر من الحد
   └─ speed_consistency: نسبة الالتزام بالحد

2. ميزات الأحداث الحادة
   ├─ harsh_brakes_count: عدد الفرملات
   ├─ harsh_accels_count: عدد التسارعات
   ├─ total_harsh_events: المجموع
   └─ events_per_minute: الشدة

3. ميزات الاستقرار
   ├─ lane_changes_count: تغييرات المسار
   ├─ lane_change_intensity: شدة التغييرات
   └─ braking_intensity: شدة الفرملات

4. ميزات السياق
   ├─ road_type_encoded: نوع الطريق
   ├─ time_of_day_encoded: وقت اليوم
   ├─ weather_encoded: الطقس
   ├─ avg_congestion: مستوى الازدحام
   └─ avg_visibility: الرؤية

5. ميزات مشتقة
   ├─ safety_index: مؤشر الأمان
   ├─ difficult_conditions: ظروف صعبة
   └─ combined_risk: المخاطر المجمعة

المجموع: 21 ميزة
```

---

## 📈 مؤشرات الأداء شرح سريع

```
Accuracy (الدقة)
= (صحيح + صحيح) / (الكل)
= "كم مرة كنت محق؟"
مثال: 92/100 = 92%

Precision (الدقة الموجبة)
= صحيح موجب / (صحيح + خطأ موجب)
= "عندما أقول 'آمن'، كم مرة أكون محق؟"
مثال: 91/100 = 91%

Recall (الاستدعاء)
= صحيح موجب / (صحيح + خطأ سالب)
= "كم نسبة الآمنين الفعليين اللي اكتشفتهم؟"
مثال: 90/100 = 90%

F1-Score
= المتوسط المتناسق بين Precision و Recall
= 0.91 (متوازن جيد)
```

---

## 🔧 استكشاف الأخطاء الشائعة

### خطأ: `ModuleNotFoundError`
```bash
# الحل:
source new/bin/activate
pip install -r requirements.txt
```

### خطأ: البيانات لا تُحفظ
```bash
# تحقق من المسار:
ls -la $(pwd)/
# ويجب أن ترى: telemetry_data.csv و trip_summary.csv
```

### خطأ: Jupyter لا يعمل
```bash
# قد تحتاج:
pip install jupyter notebook
jupyter notebook
```

### خطأ: النموذج بطيء جداً
```python
# قلل حجم البيانات:
NUM_TRIPS = 50  # بدلاً من 200
```

---

## 💡 التحسينات المقترحة

### 1. دمج بيانات حقيقية
```python
# بدلاً من البيانات الاصطناعية فقط:
- دمج بيانات الطرق الحقيقية
- دمج بيانات الازدحام
- دمج بيانات الحوادث
```

### 2. نماذج أكثر تقدماً
```python
# بدلاً من Random Forest و Gradient Boosting:
- استخدم XGBoost
- استخدم Neural Networks
- استخدم LSTM للبيانات الزمنية
```

### 3. واجهة مستخدم أفضل
```
- لوحة معلومات تفاعلية
- خريطة توضح مناطق الازدحام
- توصيات شخصية للسائقين
```

---

## 📚 موارد إضافية

### مفاهيم Machine Learning
- **Supervised Learning:** التعلم من بيانات مُصنفة
- **Classification:** تصنيف للفئات (آمن/معتدل/خطر)
- **Cross-Validation:** التحقق من جودة النموذج
- **Feature Engineering:** استخراج ميزات مهمة
- **Overfitting:** حفظ البيانات بدلاً من تعلم الأنماط

### المكتبات المستخدمة
- `pandas` - معالجة البيانات
- `numpy` - العمليات الرياضية
- `scikit-learn` - نماذج ML
- `matplotlib` - الرسوم البيانية
- `seaborn` - تصور البيانات

---

## ✅ Checklist للمشروع الكامل

- [x] توليد البيانات الاصطناعية
- [x] استكشاف ومعالجة البيانات
- [x] Feature Engineering (21 ميزة)
- [x] تدريب نموذج Random Forest
- [x] تدريب نموذج Gradient Boosting
- [x] تقييم الأداء (Accuracy: 94%)
- [ ] بناء FastAPI Backend
- [ ] بناء React Frontend
- [ ] النشر على السيرفر

---

## 📞 معلومات المشروع

**الفريق:** RoadRank Team - Hackathon Absher
**الموضوع:** Safe Driving Detection System for Saudi Arabia
**المدة:** 2025
**الحالة:** Phase 1-3 Complete, Phase 4-5 In Progress

---

**Happy Coding! 🚀**

